# Go Cinch auth backend used by the Next.js /api/auth rewrite.
AUTH_PROXY_TARGET=http://127.0.0.1:8081

# Public URL used for metadata and deployment links.
NEXT_PUBLIC_APP_URL=http://localhost:5669

# This application does not require Clerk. Sentry is optional and disabled for
# local development unless explicitly configured.
NEXT_PUBLIC_SENTRY_DISABLED=true
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_SENTRY_ORG=
NEXT_PUBLIC_SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# Optional production packaging.
BUILD_STANDALONE=false
